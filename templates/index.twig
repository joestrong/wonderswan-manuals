<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Wonderswan Manuals</title>
    <style>
        body {
            margin: 0;
            padding: 0;
        }
        .title-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1rem;
            max-width: 900px;
            margin: 50px auto 0;
        }
        .title {
            aspect-ratio: .7;
            max-width: 15rem;
            background: #ccc;
        }
    </style>
</head>
<body>
    <div class="title-grid">
        {% for title in titles %}
            <div class="title">
                {{ title.name }}
            </div>
        {% endfor %}
    </div>
</body>
</html>
